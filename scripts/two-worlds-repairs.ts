/** Explicit second (and final for this art pass) candidates. Never relax gates. */
import {createHash} from 'node:crypto';
import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {spawn} from 'node:child_process';
import {TWO_WORLD_PATCH_BOARDS,TWO_WORLD_STORAGE} from '../content/adventures/two-worlds-production';
const fixes:[string,number,string,string,string[]][]=[
 ['journey-paris-refresh-v7',1,'The return translated the original scene two native pixels and failed the immutable registration gate.','Replace ONLY the crouching striped-shirt boy. Native crop anchors: original head centre (263,307), head width about 86, hands (224,454) and (300,477), feet around y=470. Keep the same compact squat, striped cotton shirt and original ground footprint. Preserve ALL metal balls, measuring sticks, crouching neighbour and adult hands exactly.',['severeSeam']],
 ['journey-paris-refresh-v7',3,'The first return rewrote the bakery cart and bread box instead of preserving the selected child footprint.','Replace ONLY the striped shirt child behind the bicycle cart. Head centre (306,290), hands near (195,322), torso extends to (321,479). Retain striped shirt. The front bread box is CLOSED with its ORIGINAL flat lid at y=425 and cart handlebars stay at y=515. Do not invent a new basket or change the cart, bread, baker hand or seated adult. Turn only the face modestly toward the viewer.',['pictureWhole','severeSeam']],
 ['journey-tokyo-refresh-v6',2,'The original crouching boy was shrunk and the separate child behind him disappeared from the first return.','Replace ONLY the FRONT purple-hoodie boy holding the rocket. Native head centre (254,246), head width about 110; torso (211,365), crouched knees (295,452), original shoes around (290,548). Keep purple hoodie and this full footprint. CRITICAL: the separate boy BEHIND at head centre (283,139) must remain unchanged. Preserve his face, body, rocket, capsule machines and neighbouring adult. Do not collapse two children into one.',['scaleRight','pictureWhole']],
 ['journey-tokyo-refresh-v6',3,'The first return moved the child to a different counter customer and shifted the background three pixels.','Replace ONLY the BLUE SHIRT boy LEFT of the other counter girl. Native head centre (292,274), hair width about 93, hands near (195,331). Torso extends to (344,476) BEHIND the foreground smiling woman. Keep the original blue shirt and source seated body, gently turn face three-quarter without relocating it. The girl at (437,255) and foreground woman at (251,495) are NOT targets: preserve them exactly. Keep counter, pastry, chefs hands, robot and window geometry fixed.',['severeSeam','pictureWhole','childPresent']],
 ['journey-china-refresh-v8',2,'The first return moved the selected seated child behind an enlarged elder and changed the table instead of replacing the original child.','Replace ONLY the pale-pink-shirt apron child seated at the dough table in the CENTRE. Native head centre (266,309), width about 117, shoulders around y=390, hands (181,431) and (252,481), body ends behind the table at y=555. Keep pale pink sleeves and denim apron. This is a SEATED table activity, NOT a standing child behind an adult. The elder is only partially visible at the LEFT edge; leave her there and do not enlarge or move her. Preserve both right-side children, buns, fan, puppet theatre and foreground man exactly.',['pictureWhole','scaleRight','groundContact']],
 ['journey-sydney-refresh-v6',3,'The returned scene shifted one native pixel in both axes and failed the immutable registration gate.','Replace only the original child carrying the surfboard. Preserve his original standing/walking footprint, board angle and both hands gripping the same board. Keep the source outfit, ground depth, shoes or bare feet as originally drawn. No invented pose, no camera shift. All neighbouring faces, sand marks, shells, surfboard edge and shadows remain exactly as the original source.',['severeSeam']],
 ['magic-dragoncave-refresh-v3',2,'The first return enlarged the head and rebuilt the toy blocks around the child instead of preserving the original scale.','Replace ONLY the blue-shirt boy kneeling over wooden blocks. Native head centre (204,286), hair width about 146; keep the original head bounding box, do not enlarge it. Left hand (103,419), right hand (245,550), bent legs extend to (474,480). Keep this LOW leaning kneeling pose and blue cotton shirt. Preserve the block arch at x=76,y=475 and wheeled dragon toy along the bottom. Gently rotate face only enough to recognise the canonical boy.',['scaleRight','pictureWhole']],
 ['magic-dragoncave-refresh-v3',3,'The first return rewrote the source background and failed the border-difference gate at 42.6.','Replace ONLY the orange-shirt boy with apron by the small dragon on the workbench. Native head centre (268,240), width about 113, hands (176,325), both boot soles about y=586. Preserve orange shirt, apron and original body footprint. Turn head a little to three-quarter without moving it. Preserve the small green dragon, bench, other hands, floor texture, large dragon and foreground boy exactly.',['severeSeam']],
 ['magic-icepalace-refresh-v2',3,'The source image shifted three native pixels horizontally and one vertically in the first candidate.','Replace only the original child helping the ice sculptor. Keep original head centre, body size, posture and hands at the ice work table. Keep the original warm source clothing. The elderly sculptor, hands, icy workbench, carving tools, ice ornaments and every straight ice-brick edge must remain exactly at their original positions. Do not rebuild the scene to fit a new portrait pose.',['severeSeam']],
 ['magic-sweetworkshop-refresh-v3',1,'The first return shifted three pixels and replaced the chocolate train geometry along with the child.','Replace ONLY the blue-shirt boy with apron behind the chocolate train. Native head centre (279,298), hands (228,430) and (337,457), body naturally occluded by train. Keep original blue shirt and apron. Preserve EXACTLY the chocolate engine with cup on top, all wheels, nearby children, carousel, counters and bowls. Do not redesign the train to match a new pose.',['severeSeam','pictureWhole']],
 ['magic-sweetworkshop-refresh-v3',3,'The replacement became much too small and moved behind the counter, with new architecture occluding the original selected child position.','Replace ONLY the blonde girl decorating cupcakes in the FRONT of this source crop. Native head centre (276,334), head width about 122, shoulders around y=418, left hand (190,480), other hand (298,533). Keep the full original figure footprint and purple apron. Use a seated/leaning table pose at the SAME depth, NOT a tiny child in the background. Preserve cupcakes and all counter edges; do not invent an extra raised counter or wall. Preserve both other children.',['scaleRight','pictureWhole','groundContact']],
 ['magic-nightcarnival-refresh-v1',3,'The board judge rejected the first candidate for age and scale, and the original side-on mask painting action was lost.','Replace ONLY the original boy in the TOP HAT painting the fox mask. Native original face centre (278,343), head width about 115 excluding hat; keep this size with five-year-old body proportions. Keep the source waistcoat, seated side-on torso, hand at (350,495), and original fox mask at (370,433). Remove or lift the hat only enough to show canonical curls, not to enlarge head or change the pose. Preserve all masks, adjacent boy, lantern and horizontal table edge.',['ageAppropriate','scaleRight','pictureWhole']],
];
export const repairVector=(slug:string)=>[1,2,3].map(n=>fixes.some(f=>f[0]===slug&&f[1]===n)?2:1);
async function run(script:string,args:string[]){return new Promise<number>((resolve,reject)=>{const p=spawn(process.execPath,['--import','tsx',script,...args],{stdio:'inherit',windowsHide:true});p.on('error',reject);p.on('exit',c=>resolve(c??1));});}
function key(){if(process.env.OPENAI_API_KEY)return;for(const f of ['C:/GNart/Work/find-me/.env.local','C:/GNart/Work/find-me/.env']){if(!existsSync(f))continue;const m=readFileSync(f,'utf8').match(/^OPENAI_API_KEY\s*=\s*(.+)$/m);if(m){process.env.OPENAI_API_KEY=m[1]!.trim().replace(/^['"]|['"]$/g,'');break;}}if(!process.env.OPENAI_API_KEY)throw Error('Authorized key unavailable');}
async function main(){
 const phase=process.argv[2];
 if(phase==='--plan'){
  for(const [board,n,reason,support,checks] of fixes){const dir=`${TWO_WORLD_STORAGE}/${board}`,hideId=`${board}-${n}`;
   const value={inputsSha256:createHash('sha256').update(readFileSync(`${dir}/inputs.json`)).digest('hex'),hideId,attempt:2,reason,checks,placementOverride:{support:`SURGICAL ORIGINAL-SOURCE REPLACEMENT. ${support} Face and natural brown curls ONLY from canonical Image 2; keep source clothing, not the yellow hoodie from the portrait.`,occlusion:'The original image is fixed geometry. No global repaint, smoothing, relighting, camera movement or changes to neighbours. Original foreground occluders stay in front. Only the selected child identity and age-appropriate anatomy may change. The failed return is NOT a reference.'}};
   const file=`${dir}/${hideId}-repair-2.json`,data=JSON.stringify(value,null,2);if(existsSync(file)){if(readFileSync(file,'utf8')!==data)throw Error(`Frozen repair mismatch: ${hideId}`);}else writeFileSync(file,data,{flag:'wx'});
  }console.log(`Pinned ${fixes.length} explicit repairs; no purchases.`);return;
 }
 if(phase==='--render'){key();let next=0,stop=false;const worker=async()=>{while(!stop){const f=fixes[next++];if(!f)return;const code=await run('scripts/magic-bar-pilot.ts',['--two-worlds',f[0],'--render',`${f[0]}-${f[1]}`,'2']);if(code){stop=true;throw Error(`Retained repair stopped: ${f[0]}-${f[1]}`);}}};await Promise.all([worker(),worker()]);return;}
 if(phase==='--review'){key();for(const b of TWO_WORLD_PATCH_BOARDS){const v=repairVector(b.board),good=b.hides.every((h,i)=>{const f=`${TWO_WORLD_STORAGE}/${b.board}/${h.id}-attempt-${v[i]}.json`;return existsSync(f)&&JSON.parse(readFileSync(f,'utf8')).accepted===true;});if(!good){console.log(`PARKED: ${b.board}, technical candidate missing/failed; not reviewed or released.`);continue;}if(await run('scripts/magic-bar-review.ts',['--two-worlds',b.board,v.join(',')]))throw Error(`Review failed: ${b.board}`);}return;}
 throw Error('Use --plan, --render or --review');
}
main().catch(e=>{console.error(e.message);process.exitCode=1;});
