# מה באמת עלו קריאות בתשלום עד היום — קלט להחלטת התקרה (15.9.2026)

הבריף של שער הטירה (`MAGIC_CASTLE_GATE_BRIEF_20260915.md`) עוצר לפני רינדור עד ש**גיא מאשר תקרה**,
ודורש "לאמת ספק/מודל, גודל נתמך ואומדן עלות מהריצה האמיתית". זה המסמך הזה: מספרים מדודים, לא הצעה.

**לא בוצעה שום קריאה בתשלום.** הכול נקרא מהקבלות שכבר רשומות במסד הפיתוח של העץ הראשי.

## תעריף

`gpt-image-2` (`src/infra/generation/openai.ts`): טקסט־קלט $5 למיליון טוקנים, תמונה־קלט $8, תמונה־פלט $30.
הצינור של ה־QA דורש במפורש `gpt-image-2` באיכות `medium`.

## מה נרשם בפועל

| מה | מספר קריאות | סה״כ (סנט) | מינימום | מקסימום | ממוצע |
| --- | --- | --- | --- | --- | --- |
| `IDENTITY_SHEET` — גיליון הזהות של הילד | 2 | 15.00 | 7.000 | 8.000 | 7.500 |
| `TARGET_SPRITE` — מחבוא שהתקבל | 54 | 272.00 | 2.000 | 8.000 | 5.037 |
| `REJECTED_PATCH` — ניסיון שנדחה ושולם עליו | 66 | 396.00 | 2.000 | 8.000 | 6.000 |
| `TargetVariantAsset` — **מחבוא מסופק, כולל הניסיונות שלו** | 54 | 668.00 | 2.000 | 80.000 | **12.370** |

שתי מסקנות:

1. **הדחיות עולות יותר מההצלחות.** 66 ניסיונות שנדחו מול 54 שהתקבלו, ו־396 סנט מול 272. תכנון לפי
   "עלות תמונה" מחמיץ את הרוב.
2. **המספר לתכנון הוא 12.37 סנט למחבוא מסופק** — הממוצע כולל הניסיונות. המקסימום שנרשם: **80 סנט**
   למחבוא אחד.

לשלושת המחבואים האישיים של שער הטירה: **כ־37 סנט בממוצע היסטורי, עד כ־240 סנט במקרה הגרוע שנצפה.**

## המספר שחסר

**עלות הלוח הבסיסי לא רשומה בשום מקום.** כל שורות `BOARD_ART` רשומות `costCents: 0` — הלוחות הופקו
מחוץ לחשבונאות של המוצר. לכן "אומדן מהריצה האמיתית" עבור הבסיס (וההגהה האחת שמותרת) עדיין לא קיים,
והבריף דורש אותו לפני שיגור.

זה הפריט היחיד שחוסם חישוב תקרה מלאה. הוא ידרוש או קבלה אחת אמיתית מהספק, או חישוב מהתעריף מול מספר
טוקני הפלט של הגודל שייבחר — לא ניחוש.

## אזהרה תפעולית לפני שקובעים את התקרה

`GENERATION_DAILY_CENTS` **סופר את אותה הוצאה יותר מפעם אחת**
(`board-conditioned-qa-job.ts`): הוא מסכם `Asset.costCents` + `TargetVariantAsset.costCents` +
התחייבויות של ה־ledger, וההערה בקוד אומרת שזו "ספירה עליונה שמרנית" במכוון. אם תגדיר את התקרה בדיוק
לסכום שאישרת, העבודה תיעצר הרבה לפני שהוא יוצא. זה מוכר מסבב קודם ועדיין נכון.

## מה לא עשיתי

לא הצעתי תקרה, לא רינדרתי, לא שיניתי הגדרה, לא הפעלתי ספק אמיתי ולא נגעתי בפיילוט של קודקס.
ההחלטה על הסכום היא שלך.
