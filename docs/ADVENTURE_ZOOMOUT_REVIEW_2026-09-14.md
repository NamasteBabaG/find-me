# Six-board zoom-out — art review only

The parent rejected the six new boards' figure scale as too close/large, requested a modest zoom-out, and explicitly required the illustration style to stay unchanged. The parent then reiterated: finish the boards and **stop for approval before any hide work**.

## Scope and boundaries

- Revise Paris, Marrakech, Tokyo, Great Wall, Sydney and Antarctica only.
- Giza, Amazon and New York from the family-tested game remain untouched.
- Each existing new board is its own sole edit/style reference. No new aesthetic reference or style-transfer pass.
- Requested about 25% smaller linear figure scale, more surrounding activity, the same elevated shallow perspective, and no distant vanishing-point crowd. That percentage is a prompt target, not a measured per-figure guarantee.
- Preserve drawn contour character, matte paint, selective texture, palette, face construction and light/shadow treatment. Preserve the six collectible identities in each board; no personalized child is part of this art pass.
- No child render, identity purchase, hide selection, hide-coordinate authoring, game creation, progress reset, catalog replacement or deployment in this revision.

## Assets and provenance

Authoring source: `scripts/prepare-adventure-zoomout.ts`. Frozen inputs, exact prompts and PNG outputs: `output/imagegen/adventure-zoomout-20260914-v2/` (retained locally).

Generation uses the already-authorized existing API key through the bundled imagegen CLI `edit` route, `gpt-image-2`, `high`, `3840x2160`, one existing source image per request. Six distinct requests, one per board; no personal render pipeline invocation. API credentials are neither printed nor saved with the assets. The CLI does not report exact billing; no price claim is made.

Public review copies are versioned as `public/scenes/adventure-<place>-zoomout-v2/base.webp`. The previous `adventure-<place>-v1` files are retained intact. `content/adventures/expansion/zoomout-review.json` records native dimensions, source/result hashes, exact prompts and pending approval status after packaging.

## Review and safe continuation

The existing numbered gallery is updated at `/reviews/adventure-expansion-20260914.html`. Boards 1–3 remain unchanged. Boards 4–9 show the wider versions, with a “before” link to each prior 4K file. This gallery is read-only, with no player or album state.

All new art is **awaiting parent approval**, not approved for gameplay. Existing maps and personal patches remain attached to their OLD source hashes. They are not valid for these changed pixels. Do not replace board files in place or reuse the previous hide/item coordinates on the new images.

`content/adventures/expansion/review-status.json` remains present; the existing personal renderer refuses any paid work while it exists. Once this art pass finishes, base-art permission is also closed. Resume hide work only after the parent's explicit approval, then re-author the changed board geometry and recheck collectible protection before any personal generation.

## Completion checks

All six API edits completed successfully, once each. All six outputs were visually inspected: the original activity groups are smaller, their scene-specific drawing language remains recognizable, and surrounding activities were added. This is not a claim of pixel-identical style or parent approval. No second correction pass was purchased.

Packaging checked 3840×2160 output dimensions and unchanged input hashes for all six files. Browser verification loaded all nine full-resolution gallery images, found all six before-links, and detected no horizontal overflow at desktop 1600px or mobile 390px. The original three URLs are unchanged. Type-check and the four existing expansion regression tests passed; no full game/build verification was needed for this art-only review pass.

The pass ends here. No hide work follows automatically.
