# Board paint engine and passport finale — QA handoff

## Released receipt — 24 September 2026

- Released source: `6b3d9aec4b5a839df35fc61a1bfa5585108e49ad`, including passport UI commit `22ba8453`; pushed before deployment.
- Clean detached checkout: `work/qa-board-paint-release-20260924`. Deploy guard accepted the correct QA project and refreshed remote evidence. No private fixture/art files or local environment files were included.
- Final frozen-source local gate: **267 files passed, 3449 tests passed, 2 expected failures, 35 skipped**. TypeScript and both content validators passed. Existing scene-scale warnings remain in the older catalog.
- GitHub quality run **success**, including clean install, complete gate, validators, production build/private audit and no generated-source drift: https://github.com/NamasteBabaG/find-me/actions/runs/36028856674.
- Dedicated project: `find-me-qa` / `prj_LbqCRqwU8WfZpeaWU7HTXM4SsfG4`. Deployment `dpl_Hn5SnRX5bhNx8rx95qrEsEuTU2Mj`, **READY**: https://find-me-fu69yyd2f-smallheroes-projects.vercel.app.
- Remote Next.js build and tracing audit passed: `privateLeaks: []`, `problems: []`; Vercel inspected function size 181.11 MB. Build logs confirm simulated payments.
- Built with `--skip-domain`, inspected, then promoted. Alias API independently confirms https://qa.findmeworlds.com points to that deployment and QA project. Deployment API `meta.gitCommitSha` and `meta.releaseCommit` both match the released source above.
- Live browser navigation reaches the expected password gate; access protection remains enabled. The session is not logged in, so **no fresh authenticated live-game or health walkthrough is claimed**. User was asked to sign in without sharing the password. Local completion walkthrough evidence is linked below.
- Post-promotion deployment-filtered error query returned no logs; this is a small observation window, not production-load validation. No public-store deployment or database mutation was performed.
- This receipt is a later documentation-only commit; it does not change the deployed application SHA.

## Requested scope

Integrate the user's approved second Bar dragon-stool style direction into the actual generation engine, then publish the engine and bound-passport completion UI to the dedicated QA project. This is not permission to overwrite every delivered personalized image or activate a different paid catalog.

## Engine change

- New recipe: `local-patch-prompt/v12-board-paint-identity`, selected by the production hide runner when it creates a new variant row.
- Image 2 determines identity geometry, hair pattern and age. The original board people determine surface painting, warm/cool facial planes, brush-detail scale, material response and local light. Neighbour identities must not transfer.
- Explicit whole-body scale, physically plausible floor/prop contact, natural activity/gaze, preserved edit boundaries and discovery objects.
- Repair instructions preserve the same separation; repairing likeness must not reintroduce smooth portrait rendering.
- No noise filter, global image treatment, quality-gate weakening, changed attempt allowance or budget increase.
- New prompt/version is part of the existing paid-operation fingerprint. Existing variant rows retain their historical recipe; replay cannot reinterpret a prior purchase under the new recipe. Already generated/approved rows return without another purchase.
- Historical direct renderer callers retain their existing default unless they explicitly select `paintRecipe: "board-paint-v1"`. This includes frozen offline rendering scripts.

The approved image is a visual reference, not evidence that every image generated by this revised prompt will pass visual acceptance. A fresh real-provider sample and contextual review remain necessary before mass rendering.

## Passport UI included

Commit `22ba8453` changes board completion into a bound passport using shared paper/binding materials, two desktop leaves and compact mobile layout. Existing stamp timing/audio, reduced motion, save acknowledgements and navigation are preserved. Local real-pointer walkthrough and five viewport measurements are recorded in `passport/BAR_STYLE_AND_COMPLETION_20260924.md`.

## Tests and release evidence

Targeted tests cover new recipe dispatch/persistence on real disposable SQLite, historical row continuation, retained-purchase refusal on a changed fingerprint, replay without a second provider call, age/reference invariants and canonical identity reuse. The reuse integration assertion was updated from an old uppercase heading to explicit identity and paint authority clauses; its image/reference and billing assertions remain intact.

The first full run suffered a roughly ten-hour wall-clock discontinuity and four timeout failures. It also exposed the old heading assertion and its cascading mock-call failures. Those results are not counted as a passing gate; the subsequent complete gate and release receipt are reported separately.

## What this release does NOT update

- The existing 54 personal appearances and their progress/configuration are not rewritten.
- The approved repaired third dragon appearance remains in its separate local proof game; it is not automatically published as QA art by deploying code.
- No schema migration, public-store deployment, new paid rendering or catalog activation is included.

## Independent Claude challenge

1. Start from the exact released SHA, not a dirty implementation worktree. Verify the QA alias and build provenance independently.
2. Force a new hide through the real runner with synthetic provider adapters: assert v12 reaches the painter and is persisted. Resume a historical row and a retained purchase; neither wording nor charged operation may silently change.
3. Review the new prompt for identity drift, overly strong brush texture, scale and furniture contact. Compare actual new paid samples at native size with the approved reference before recommending bulk generation.
4. Replay the completion UI in Hebrew/English, desktop/short laptop/mobile, reduced motion, muted sound, delayed/failed save and final-board navigation. Check actual dialog overflow, not only document overflow.
5. Confirm delivered games are untouched and no claim of 54 rerendered appearances is made. Report findings with reproduction and severity; do not deploy or render in this review without separate authorization.
