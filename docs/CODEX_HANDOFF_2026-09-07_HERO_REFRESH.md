# החלפת העותקים בהירו — 7 בספטמבר 2026

## מה השתנה

- 27 הבורדים החדשים כבר הוכנסו לקטלוג בקומיט `04495d4` ונפרסו ל־QA.
- נמצא חריג: ההירו החזיק שלושה עותקים עצמאיים ישנים ב־`public/home/`.
- שלושת הקבצים נדרסו בנגזרות של הבורדים החדשים: ניו יורק, מערת הדרקון ועיר העתיד. ללא קריאות יצירת תמונה בתשלום.
- גם שכבת הרקע החיוורת וגם שכבת הפנס קוראות את אותו manifest. כתובת כל תמונה כוללת hash למניעת מטמון ישן.
- המעבר הוא כל 12 שניות במקום 7; משך ההצלבה הקיים לא השתנה. במצב reduced motion אין החלפת בורדים.
- הטאג בהירו הוחלף בתווית אפורה־חמה, ללא צהוב, צל או מראה של כפתור.
- דמו במובייל לאורך: חלון חיפוש בגובה 78svh (420–760px), עם גרירה לרוחב. ברוחב: גובה המסך פחות 60px, ניווט 52px וכפתורים בגובה נגיעה 44px.
- שינוי כיוון המסך שומר על אזור החיפוש והזום היחסי ולא על גודל פיקסלים מיושן; לרוחב בטלפון הציור ממלא את רוחב החלון.
- הוסר הפס הגרפי הגלובלי של QA בלבד. סיסמת QA, noindex, חסימות Server Actions והפרדת ספקי התשלום נשארו.
- `scripts/refresh-hero-art.ts --apply` מחדש את העותקים מתוך הגדרות הסצנות העדכניות ומוודא את hash המקור.

## גבולות

כרטיסי המקומות בקרוסלה, מפות המשחק ותיק ההרפתקאות קוראים את הארט/thumbnail מהקטלוג. מפות העולמות הן איורים נפרדים, לא עותקים של בורד. דמו החוף ושלב ההדגמה בדף הנחיתה הם סצנת `beach` נפרדת ואינם אחד מ־27 הבורדים שהוחלפו.

לפי ההבהרה האחרונה של גיא, אין להשקיע בהסבת משחקי הבדיקה שכבר נוצרו. לא נמחקו משחקים או נתונים, ולא נוספה עבודת תאימות עבורם בסבב הזה.

## בדיקה

- שתי שכבות × שלושה קבצים נטענות בפועל בדפדפן המקומי עם הכתובות החדשות.
- דסקטופ 1440×1000 ומובייל 390×844: ללא overflow אופקי וללא שגיאת טעינת תמונה בהירו.
- בדיקה משחזרת את נגזרות התמונות מהמקור ומשווה בייטים, hash, גרסת סצנה וכתובת cache-busting.
- בדיקת React עם שעון נשלט: לפני 12 שניות אין מעבר; ב־12 שניות שתי השכבות מתחלפות יחד; אחרי מחזור חוזרים להתחלה; unmount מנקה timer.
- reduced motion נשאר סטטי.
- צילומים: `output/slot-review-20260907/hero-refreshed-desktop.png` ו־`hero-refreshed-mobile.png`.

## לבודק השני

לבדוק על QA אחרי רענון מלא: ניו יורק → מערת הדרקון → עיר העתיד, 12 שניות לכל בורד, ותמונה זהה ברקע ובפנס. לא להפוך את ההחלפה החזותית לאישור איכות המחבואים: תכנון Astra/חיתוך Sol עדיין פיילוט נפרד, ואין עדיין הסמכה ל־81 מיקומים.
