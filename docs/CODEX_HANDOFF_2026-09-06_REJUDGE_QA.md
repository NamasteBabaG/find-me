# מסירת Codex ל־QA עצמאי של Claude — 6.9.2026

## חלוקת עבודה וגבולות

Codex מנהל ומיישם; Claude בודק שני. הסבב הזה ממשיך את `cf17100`, ואינו סקירת UX מחודשת של כל המוצר.

**מצב סופי של הקוד בסבב: QA מריץ `a65d99f`, הכולל את תיקון השיפוט `92820db`. שיפורי ה־UI שקלוד מבצע במקביל אינם כלולים בפריסה הזאת.** אין שינוי בקובצי הבית, CSS, רכיבי המשחק או Shell של קלוד. השינוי היחיד במסך לקוח הוא מנגנון התשאול ב־CreatingStatus, ללא שינוי בתצוגה. נא לתאם פריסות ל־QA כדי לא לדרוס גרסה שנבדקת.

אושר שיפוט מחדש של F עד 4 סנט, פריסה ל־QA, ובחינה של הזזת הפנסים. לא אושרו כאן רנדרי תמונה חדשים, פתיחת החנות או הפעלת B. קבצי ניסוי ותמונות אישיות נשארים מקומיים, מחוץ לריפו הציבורי ולפריסה.

## מה קובע הקומיט

קומיט המימוש: `92820dbdc6f151d3ae3fa8d64546504499cc373d` — נדחף ל־GitHub.

- השופט מתעד כל ניסיון HTTP: usage, מזהה בקשה, המודל שחזר, הסטטוס, הטקסט והפרומפט. כשנדרשים שני ניסיונות, שניהם נספרים.
- usage חסר, תשובה לא קריאה או כשל תעבורה אינם קריאה חינמית. לא מתבצע retry אוטומטי אחרי חיוב לא ידוע.
- ניסויי דגימה ו־rejudge משתמשים בניסיון HTTP אחד לכל שיפוט.
- `rejudge` מאמת מראש את הפיקסלים ואת קובצי המקור מול hash, ומשתמש בגיליון מלא מארכיון E של אותו סבב. הוא אינו נשען על גיליון חי שאולי השתנה.
- תיקיית פלט חדשה בלבד; גם תיקייה קיימת וריקה נדחית. לפני בקשה נשמרת רזרבה לדיסק. חיוב לא ידוע נשמר כרזרבה ועוצר את הריצה.
- נתוני השופט ו־costUnknown נשמרים גם בצינור המוצר ובכלי הדגימה.
- `.vercelignore` מוציא במפורש גם `output/`, `tmp/` וקובצי PDF בשורש. הקבצים עצמם לא נמחקו.

גבול חשוב: אין כאן מערכת תקציב גלובלית חוצת־ריצות. אחרי קריסה או חיוב לא ידוע אסור לפתוח תיקיית ריצה חדשה ולנסות שוב ללא בירור החיוב הקודם. סכומי usage מחושבים לפי התעריף הרגיל, ללא הנחת הנחת cache; הם אינם התאמה לחשבונית ספק.

## תוצאת השיפוט החוזר — לא רינדור חוזר

| מדד | תוצאה |
| --- | --- |
| מכנה | 16 תאים היסטוריים |
| טלאים קיימים שנשלחו לשיפוט | 15 |
| כישלון צורה שנשאר במכנה ללא שיפוט | 1 |
| אישור השופט המקורי, מול רפרנס הראש | 7/16 |
| אישור השופט החדש, מול הגיליון המלא | 10/16 |
| שינוי bad → ok | 3 |
| שינוי ok → bad | 0 |
| מספר בקשות HTTP בפועל | 15 |
| עלות מחושבת לפי usage | 3.889005 סנט = $0.03889005 |
| תקרת אישור | 4 סנט |
| חיובים לא ידועים / בקשות תלויות | 0 / 0 |
| מודל שהחזיר הספק | gpt-4o-mini-2024-07-18 |
| אי־התאמות hash במקור אחרי הריצה | 0 |

כל שלוש ההחלטות שהשתנו היו באותה זהות: הקאנו בחזרה הראשונה, והגלשנים בשתי החזרות. בזהות האחרת נשארו 7 אישורים מ־7 טלאים כשירים, ועוד כישלון הצורה במכנה.

ראיות מקומיות, לא לפרסום:

- `work/patch-quality/f-codex-live-20260906/plan.json`
- `work/patch-quality/f-codex-live-20260906/results.json`
- המקור נשאר ב־`work/patch-quality/e3/`; לא נדרסו manifest, cell.json, דירוגים או תמונות.

### מסקנה ניהולית

רפרנס השופט אכן השפיע על התוצאה, אבל לא סגר את הפער מול 14/16 בדירוג החזותי הקודם של Claude. זה אינו ניסוי שמוכיח ש־F עדיף על E: זהות השופט אינה אמת מידה אנושית, אין כאן רנדרים חדשים, יש רק שתי זהויות וייתכנו שינויי ספק/שונות בין קריאות.

הגיליון המלא נשאר ברירת המחדל. אין להסיק שהשופט צדק בכל 5 הפסילות שנותרו, ואין להפוך אישור שופט לאישור מכירה. הצעד הבא הוא בדיקה חזותית על הלוח, עם דגש על התאים שבהם הדירוגים חלוקים, לפני ניסוי נוסף בתשלום.

התעריפים ורזרבת התמונה נבדקו מול [תמחור GPT-4o mini](https://developers.openai.com/api/docs/models/gpt-4o-mini) ו[חישוב עלות תמונות](https://developers.openai.com/api/docs/guides/images-vision). ההזמנה מניחה את התעריף המלא, ללא הנחות cache.

## פריסת 92820db ובדיקת ריצה ראשונה

- היעד הוא **find-me-qa בלבד**, project ID: `prj_LbqCRqwU8WfZpeaWU7HTXM4SsfG4`.
- [סביבת QA](https://find-me-qa-smallheroes-projects.vercel.app).
- Deployment של שלב זה: `dpl_CupTD2jr1Gx6htdY55oNPqTZawcv` (הוחלף בהמשך בתיקון התשאול, להלן).
- הפריסה נעשתה מ־worktree מבודד על `92820db`, ללא השינויים המקבילים בדף הבית.
- build מרוחק עבר. `APP_COMMIT` הוזן במפורש בזמן הפריסה; בדיקת health מחזירה `92820db`, `appEnv: qa`, DB תקין, OpenAI ליצירה, mock לתשלום ו־Resend למייל.
- פריסה ראשונית של אותו קוד הייתה תקינה אך health הציג commit ריק. בוצעה פריסה שנייה עם חותמת מפורשת. מזהה הפריסה לעיל הוא הסופי.
- מסכי האדמין והספרייה נפתחו בחשבון הבדיקה הקיים. קיימים שני משחקים שנמסרו בעבר; אין הזמנות בתהליך יצירה בזמן הבדיקה. זו אינה הוכחה למסירה חדשה מהגרסה הזאת.
- cron של הפריסה הסופית: `GET /api/jobs/tick` חזר **200 ב־10:10:37 (ישראל)**, לפי לוג שסונן ל־deployment הסופי. אזהרת הקונטיינר על תשלומי mock צפויה ב־QA.
- **בדיקת המסירה נכשלה בגרסה זו**: בקשת resend יחידה למשחק QA קיים חזרה עם שגיאת DB. בלוג 10:11:51 מופיע `P2024` ב־`shareLink.findFirst`, לפני הפנייה ל־Resend. באותו חלון זמן נצפו שגיאות pool בבקשות status/assets/session. אין להסיק מ־`health: email=resend` שהמייל נשלח.
- החנות נשארת מושהית. לא שונו הרשאות מכירה, דגל B, מפתחות API או הסכמות DB.

## בדיקות קוד

- `npm run check`: 39 קבצים, 253 בדיקות עוברות, TypeScript נקי. 23 בדיקות נוספו בסבב.
- הרצה חוזרת ב־worktree המבודד: 39/253, exit 0, בשעה 10:07; כ־43 שניות.
- `npm run scenes:validate`: 36 סצנות תקינות. אזהרה קיימת בסצנת beach הלא פעילה על scale גדול; לא הוסתרה ולא תוקנה בסבב זה.
- `git diff --check` עבר. הקומיט כולל רק תשעת הקבצים שנקראו ונבחרו בשם; ללא `git add -A`.
- הבדיקות הממוקדות כוללות: שני חיובים בשני ניסיונות; missing usage; timeout; JSON פגום/null; מודל ללא תמחור; רזרבה לפני HTTP; תקציב שלא מספיק לקריאה הראשונה; עצירה בחיוב לא ידוע; סירוב לדריסה; קלט שהשתנה; פרטיות נתיבי פלט; והעברת מידע העלות דרך צינור SQLite אמיתי עם ספקים מדומים.

## פנסי החומה — נשאר פתוח, לא סומן כמתוקן

נבדקו ארט המקור ותצוגת `/admin/scenes/greatwall` ב־QA. הנקודה הקיימת A, `(0.315, 0.505)`, יושבת באזור הדרקון. ההצעה `(0.370, 0.400)` קרובה מאוד לדמות קיימת ולפנס. היא מועמדת לבדיקה, לא מקום ריק שהוכח שמתאים.

לא שונו הקואורדינטות או גרסת הסצנה. לא הופק רנדר חדש. אישור תמונת cut-out ב־rejudge לא מוכיח שהטלאי נמצא באזור לחיצה נכון, משתלב בהסתרה או נראה טוב בתוך הלוח.

המלצה: לבחור חלופה עם גוף/ראש באזור חופשי, לבדוק יחד scale, חלון 4×, מסכה ו־hintZone, ואז רנדר מצומצם והלבשה חזרה על הלוח. רק אחרי מעבר כזה מעדכנים scene version. לא לרכך את השופט כדי לפצות על עוגן שגוי. אין כעת אישור תקציב לרנדרי הבדיקה האלה.

ממצא צד בקופי האדמין: `שלוש פעמים בהחומה הסינית` צריך להיות `שלוש פעמים בחומה הסינית`. לא שונה בתוך סבב תשתיות זה.

## משימת Claude: QA, לא יישום מקביל

1. לאמת את `92820db` כבודק שני. לדווח P1/P2 עם שחזור, קובץ ושורה. לא לערוך את אותם קבצים במקביל בלי תיאום.
2. לבדוק במיוחד את אומדן הרזרבה: שני קלטי תמונה עד 512px, max_tokens=60, ניסיון HTTP יחיד. לבדוק תרחיש עלות מעבר לרזרבה ומודל מפתיע; העצירה היא guard, לא תחליף לבקרת חיוב אצל הספק.
3. לבדוק שה־15 request IDs ו־usage נשמרו, שסכומם מתאים ל־3.889005, שאין pending, ושקובצי המקור תואמים ל־hash. לא להריץ `--go` מחדש: אישור 4 הסנט נוצל כמעט כולו.
4. לבדוק שהדיווח 10/16 לא נכתב בטעות כ־10/15, ושאף תוצאה חדשה לא החליפה את דירוגי הניסוי המקורי.
5. לבדוק את העברת `costUnknown` הן לענף קבלה והן לענף דחייה בצינור, ואת ההבדל בין סכום ידוע חלקי לבין עלות מלאה ידועה.
6. לבדוק שליחת התראות אחרי כשל ספק ודדופליקציה; בדיקות in-memory ו־SQLite אינן מדמות את כל תנאי ההתרסקות והמקביליות ב־Vercel.
7. לבצע סקירה חזותית של התאים שבמחלוקת ושל הצעת הפנסים, על הלוח ובזום. לציין שמדובר בבדיקת מודל ולא בילד/ה או אדם אמיתי.
8. לא לשנות את ברירת המחדל של רפרנס, לא לפתוח מכירה, לא להפעיל B, לא לפרוס לחנות ולא לבצע רנדרים חדשים בתשלום כחלק מה־QA הזה.

## ממצא ריצה נוסף ותיקון עומס ממוקד

לאחר בדיקת health מוצלחת ו־cron 200, פתיחת אדמין המשחק, הספרייה ומסך ההמתנה חשפה מיצוי pool. הלוג מציין `connection limit: 1`, ו־`pool timeout: 10`. בבקשת השליחה החוזרת הכשל אירע לפני שירות המייל. אחרי יציאה ממסך ההמתנה health חזר תקין בכ־573ms. זהו כשל בעומס, לא הוכחה למפתח Resend שגוי.

בקוד `CreatingStatus` נמצאו שני גורמי עומס מאומתים: `setInterval(2500)` גם אחרי READY/DELIVERED; והיעדר נעילה על בקשת הסטטוס (הנעילה הקיימת מגינה רק על נגיעת ה־job). כתוצאה מכך, בקשה שמתעכבת יכולה לחפוף לבקשות נוספות. גם כתובת האווטאר נחתמת מחדש לפי שנייה, כך ששינויה גורר קריאות תמונה נוספות דרך ה־DB.

נוסף poller טורי: קריאה אחת לכל היותר, השהיה אחרי סיום הקריאה, ביטול בקשה תלויה ביציאה, deadline של 15 שניות, ועצירה כשהמשחק מוכן או במצב סופי. נגיעת ה־job נשארת נפרדת מקריאת הסטטוס, כדי שלא להקפיא את ההתקדמות בזמן slice ארוך. אין שינוי JSX, עיצוב או קופי. נבדקו גם הרכיב המורכב בפועל ומסלול job ארוך, לא רק פונקציית העזר.

קומיט תיקון התשאול: `a65d99f96b3128cbf635d086a647974a2cdf38f3`, נדחף ל־GitHub. `npm run check` עבר עם **41 קבצים / 260 בדיקות**; שבע הבדיקות החדשות עברו שוב גם בעותק המבודד. זהו הקומיט האחרון של הקוד שנפרס בסבב הזה.

Deployment חדש: `dpl_DJUBNaAeRDvaQpq47SDqvLtR4Lkr`, מאותו פרויקט QA. build עבר. health מזהה `a65d99f`, DB תקין, `appEnv: qa`. התיקון כולל גם החרגה מפורשת של `work` כשהוא junction, ולא רק כשהוא תיקייה. בדיקת עץ קובצי הפריסה מצאה אפס רשומות שורש של work/storage/output/tmp וקובצי env פרטיים.

אימות חי אחרי הפריסה:

- health: `a65d99f`, DB תקין, כ־563ms במדידה שנשמרה.
- cron של הפריסה החדשה חזר 200 ב־10:25:37. בשאילתת שגיאות לפריסה החדשה לאחר בדיקת המסירה לא חזרו לוגים ברמת error; זו בדיקת חלון קצר, לא ניטור מתמשך.
- מסך של משחק שכבר נמסר נשאר פתוח מעל דקה. בלוג הפריסה נראתה קריאת status אחת ב־10:25:10, וקריאת אווטאר אחת ב־10:25:14; לא גל בקשות כל 2.5 שניות.
- ב־10:26:01 בוצע resend שני, אחרי שהכשל הראשון הובהר ככשל DB לפני המייל. הבקשה חזרה 200 והמסך הציג **„נשלח ✓”**. זו הוכחה שהאפליקציה קיבלה הצלחת ספק; לא נבדקה הגעה בפועל לתיבת Gmail.
- קישור המשחק הקיים נפתח למפת 9 המקומות עם ההתקדמות הקיימת 9/9. לא אופסה ההתקדמות ולא נוצר משחק חדש.
- צילומים מקומיים ב־`output/codex-92820db-qa/`: `delivery-failed.png`, `delivery-sent.png`, `existing-game-map.png`, `greatwall-admin.png`. אין להוסיף אותם לריפו הציבורי.

ממצא UI למסירה לקלוד, בלי שינוי מצדי: במפה של משחק עם 9/9 חותמות עדיין מוצג „המקום הבא — אנטארקטיקה”. כדאי לבדוק מצב סיום מובחן. זו תצפית על הגרסה שלפני שיפורי ה־UI המקבילים; ייתכן שקלוד כבר מטפל בכך.

**אין טענה שכל בעיית החיבורים נפתרה.** לא שונתה מגבלת החיבורים, לא הוחלף שירות אחסון, ולא בוצעה בדיקת עומס של כמה משפחות במקביל. נדרש סבב מדידה נפרד לטעינת הרבה assets באדמין ול־DB storage, לרבות אימות התאמת ה־pool והאזור של Vercel. הנחיות Next.js/Supabase שימשו להפרדת בעיית התשאול המקומית מהחלטה תשתיתית שדורשת מדידה; לא הוגדל pool באופן שרירותי.

בתור בודק שני, Claude צריך לוודא במיוחד: 30 שניות במסך של משחק DELIVERED אינן מייצרות גל GET כל 2.5 שניות; קריאה איטית אינה חופפת לאחרת; visibilitychange אינו מכפיל בקשות; יציאה מהרכיב מבטלת קריאה; ומסך משחק שנוצר עדיין מתעדכן בזמן נגיעת job ארוכה.

## נשאר לסבב הבא בניהול Codex

P1: מחבוא פנסים שעובר גם מיקום/לחיצה וגם בדיקת לוח; כיסוי חזותי לפני מכירה. P2: תיאור wardrobe/action/expression ב־23 הסצנות שטרם קיבלו אותו; כיסוי regression לשופט ולכלי הדגימה מעבר לתיקונים הנוכחיים; ורק בהמשך תיקוני UI/קופי נפרדים אחרי שהעבודה המקבילה בדף הבית תסתיים.

זו מסירת גרסה ל־QA, לא אישור שהמוצר מוכן למכירה. לא נוצר כאן משחק OpenAI חדש מקצה לקצה, לא בוצעה בדיקת משתמשים עם ילדים, ולא נבדקו כל מקומות B.
