# Same-sheet observer recovery

One optional second Sol HIGH observation is now available for an already-paid
board-specific sheet. No image provider is present in the recovery command.
The original source, source request, first measurement, and first bill are not
overwritten. There are at most two observations per source.

The shared engine requires the original exact source/contract, same-sheet raw
first-observer receipt and its durable settled charge before accepting
`measurementAttempt: 2`. It uses the distinct `board:<board>:measure:2` request
and independently immutable measurement checkpoint. All extraction, face,
crown, feet, scale and composition guards still run normally. The second result
carries its actual durable charge for player/review provenance. Default first
observation output and keys remain unchanged.

## Antarctica command, approved receipt required

The free preflight already succeeded on this exact source/spec:

```powershell
npx tsx scripts/board-conditioned-reobserve.ts --result=work/board-conditioned-engine-20260909/world-final-antarctica-v2 --spec=work/open-placement-20260909/world-runtime-v2/antarctica/spec.json --continuation-approval=work/open-world-hardening-20260909/CONTINUATION_APPROVED_USER_21.json --run
```

Only the parent agent dispatches this paid command, after the shared paid lock
is free. Omit `--run` for free preparation. The prior observer cost11.509c;
the additional request reserves40c, not a guaranteed price. It uses the same
hardening ledger/cap and cumulative ceiling. The approved old20c unknown reserve
remains counted; a new unresolved response creates a new hold.

Output is `world-final-antarctica-v2/measurements/second-observation-v1` beneath
the private engine work directory. A crash after a retained second response can
re-derive it without redispatch. Missing/unknown evidence cannot be purchased
again through this command. A completed result blocks another paid invocation;
there is no third-measurement option.

No manual landmark corrections, enlarged crown tolerance, repainted source,
automatic semantic approval, or guaranteed recovery is implied. The real wizard
uses a later tick for eligible observer-only failures, avoiding another full
source+observer sequence in the same server request; its per-source retry remains
bounded and its player binding identifies the chosen measurement charge.

## Recorded result and delivery boundary

The one second observation completed for 14.529c with request
`req_b74356604f124f12858be2a35976399a`; the first observation remains unchanged.
Its original-contract result is still diagnostic at the old placements. The
separate `destinations/geometry-v3-measurement-2` replay passes all three geometry
checks using revised fixed placements and the same paid sheet. That replay is
not a player-bound game and must not be relabelled as the original contract.
The final catalog and actual new-game wizard are the supported delivery path;
creating and playing a new authenticated QA game remains the end-to-end proof.
Neither a geometry pass nor receipt verification implies visual approval.
